services:
  url_shortener:
    build:
      context: .
      dockerfile: Dockerfile
    image: url_shortener:latest
    ports:
      - "5000:5000"
    environment:
      FLASK_ENV: development
    container_name: url_shortener
